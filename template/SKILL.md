---
name: template-skill
description: 一句話講這個 skill 做什麼 + 什麼時候該用（what + when）。第三人稱、稍微 pushy。Triggers - "<中文觸發語>"、"<english trigger>"、"/template-skill"。
---

# Template Skill

> 複製本資料夾改名（目錄名 = frontmatter `name` = kebab-case），照 [CONTRIBUTING.md](../CONTRIBUTING.md) 填，
> 再加進 `.claude-plugin/marketplace.json`（自身 plugin + bundle）。刪掉這段引言。

一段話：這個 skill 解決什麼、讀完要能直接做。prose 用中文，專有名詞英文＋首次解釋。

## 什麼叫好（可選但建議）

說清楚「過關」的標準是什麼 —— 不是把範本填滿，而是達成真正的目的。

## 步驟 / 決策樹

1. ...
2. ...

（太長 → 拆進 `references/`，相對路徑只深一層；工具放 `scripts/`，純 stdlib 無相依。）

## 完成定義

- ...
